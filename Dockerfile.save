# 基础镜像（以轻量的 alpine 版本为例，适配大多数场景）
FROM alpine:latest

# 维护者信息（可选，可删除）
MAINTAINER your-name <your-email@example.com>

# 设置工作目录（后续命令都在这个目录执行）
WORKDIR /app

# 复制本地项目所有文件到容器的 /app 目录
COPY . /app

# 暴露端口（根据你的应用调整，比如 web 应用常用 80/3000/8080）
EXPOSE 80

# 容器启动时执行的命令（根据你的应用修改，比如启动脚本/主程序）
CMD ["/bin/sh", "-c", "echo 'App started!' && sleep infinity"]
